import 'package:flutter/material.dart';

class AppColors {
  // ── Brand ──────────────────────────────────────────────────────────────────
  static const Color primary = Color(0xFF00450D);
  static const Color onPrimary = Colors.white;
  static const Color primaryContainer = Color(0xFF1B5E20);
  static const Color onPrimaryContainer = Color(0xFFB7F0B1);

  static const Color secondary = Color(0xFF4E6B47);
  static const Color onSecondary = Colors.white;
  static const Color secondaryContainer = Color(0xFFCAE8C2);
  static const Color onSecondaryContainer = Color(0xFF0C2009);

  static const Color tertiary = Color(0xFF8B6F00);
  static const Color onTertiary = Colors.white;
  static const Color tertiaryFixed = Color(0xFFFFE170);
  static const Color onTertiaryFixed = Color(0xFF221B00);

  // ── Accent ─────────────────────────────────────────────────────────────────
  static const Color accentYellow = Color(0xFFFFD600);

  // ── Surface & Background (Light) ───────────────────────────────────────────
  static const Color background = Color(0xFFF7FBF1);
  static const Color surface = Color(0xFFF7FBF1);
  static const Color surfaceContainerLow = Color(0xFFF2F5EC);
  static const Color surfaceContainerLowest = Colors.white;
  static const Color surfaceVariant = Color(0xFFE0E4DB);

  // ── Surface & Background (Dark) ────────────────────────────────────────────
  static const Color darkBackground = Color(0xFF111318);
  static const Color darkSurface = Color(0xFF1A1C22);
  static const Color darkSurfaceContainerLow = Color(0xFF1E2028);
  static const Color darkSurfaceContainerLowest = Color(0xFF16181E);
  static const Color darkSurfaceVariant = Color(0xFF252830);

  // ── Text & Content (Light) ─────────────────────────────────────────────────
  static const Color onSurface = Color(0xFF191D17);
  static const Color onSurfaceVariant = Color(0xFF41493E);
  static const Color outline = Color(0xFF717A6D);
  static const Color outlineVariant = Color(0xFFC0C9BB);

  // ── Text & Content (Dark) ──────────────────────────────────────────────────
  static const Color darkOnSurface = Color(0xFFE2E2E9);
  static const Color darkOnSurfaceVariant = Color(0xFF9A9DB0);
  static const Color darkOutline = Color(0xFF44464F);
  static const Color darkOutlineVariant = Color(0xFF2E3038);

  // ── Functional ─────────────────────────────────────────────────────────────
  static const Color error = Color(0xFFBA1A1A);
  static const Color onError = Colors.white;
  static const Color errorContainer = Color(0xFFFFDAD6);
  static const Color onErrorContainer = Color(0xFF410002);

  static const Color success = Color(0xFF2E7D32);
  static const Color onSuccess = Colors.white;
}
